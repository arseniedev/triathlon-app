// import Stopwatch from './stopwatch.js'

class Duration { //extends Stopwatch {
    constructor(
        newSport,
        newDuration = 0.0,
    ) {
        this.sport = newSport
        this.duration = newDuration
    }
}